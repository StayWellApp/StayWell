{
  "name": "firestore-tests",
  "version": "1.0.0",
  "description": "Firestore rules tests",
  "main": "index.js",
  "scripts": {
    "pretest": "cp ../../firestore.rules .",
    "test": "firebase emulators:exec --only firestore 'jest'",
    "test:direct": "jest"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0",
    "@firebase/rules-unit-testing": "^3.0.0",
    "jest": "^29.0.0",
    "firebase-admin": "^11.0.0"
  }
}
